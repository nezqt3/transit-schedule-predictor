# МСК Хакатон транспорта — прогнозирование задержек общественного транспорта

Проект команды для **МСК Хакатона транспорта**. Решение собирает телеметрию транспортных средств, объединяет её с расписанием и строит прогноз задержки рейса или прибытия на целевую остановку.

> Статус: MVP-каркас. Основные контуры репозитория разложены, ML- и сервисные точки входа подготовлены для дальнейшей реализации и обучения.

## Команда

- **Алексеенко Денис** 
- **Верещагин Илья**
- **Урманов Артём**

## Бизнес-идея

Городской транспорт теряет время и деньги не только из-за пробок, но и из-за того, что диспетчер видит проблему уже после возникновения задержки. Наша задача — превратить поток телеметрии в раннее предупреждение:

```text
телеметрия ТС + расписание + история отклонений
                    ↓
          признаки текущего рейса
                    ↓
       ML-прогноз задержки и класса
                    ↓
 диспетчер / пассажирский сервис / аналитика
```

### Что получает транспортный оператор

- заранее видит рейсы, которые с высокой вероятностью выйдут из расписания;
- может перераспределить резервный транспорт и водителей до того, как проблема станет массовой;
- получает единый слой данных для контроля маршрутов, остановок и фактического исполнения расписания;
- сокращает ручной разбор телеметрии и постфактум-отчётность;
- может оценивать качество маршрутов по фактической задержке, а не только по плану.

### Что получает пассажир

- более точное ожидаемое время прибытия;
- предупреждение о задержке до поездки;
- меньше неопределённости при пересадках и выборе маршрута.

### Экономический эффект: «что по чём»

На этапе хакатона мы не подменяем реальные расчёты выдуманной экономией. Модель эффекта считается от данных оператора:

| Показатель | Как считаем | Зачем нужен |
| --- | --- | --- |
| средняя задержка | фактическое время − плановое время | базовая линия качества |
| доля опоздавших рейсов | рейсы выше порога / все рейсы | масштаб проблемы |
| раннее предупреждение | время до фактической задержки | полезность прогноза |
| precision/recall по критическим задержкам | прогноз против labels | качество ML |
| стоимость инцидента | простой, штрафы, жалобы, сорванные пересадки | деньги оператора |
| экономия | предотвращённые инциденты × стоимость одного инцидента | ROI внедрения |

Коммерческая модель после пилота: SaaS/API для транспортного оператора или городского интегратора с оплатой за парк, маршрут или объём обработанной телеметрии. Дополнительные продукты — аналитика исполнения расписания, мониторинг SLA и пакет прогнозов для пассажирского приложения.

## Как работает решение

1. NDTP-эмулятор отправляет телематические пакеты: координаты, скорость, курс, датчики и идентификатор борта.
2. Backend принимает поток, разбирает NPL/NPH-пакеты и предоставляет API транспортных объектов.
3. ML-контур объединяет traffic, schedule и labels, строит временные и маршрутные признаки.
4. CatBoost и PyTorch обучаются на исторических данных; ансамбль объединяет прогнозы.
5. FastAPI ML-сервиса возвращает секунды задержки, класс задержки и версию модели.
6. Результат может использоваться диспетчерским интерфейсом, уведомлениями и отчётами.

## Архитектура репозитория

```text
data/                         Датасеты train/test/validate и sample submission
emulator/                     NDTP-эмулятор и спецификация протокола
backend/                      API, TCP/NDTP parser, telemetry и ML client
ml/
  src/dataset.py              загрузка и проверка данных
  src/features.py             генерация признаков
  src/preprocessing.py        общий preprocessing без target leakage
  src/models/                 CatBoost, PyTorch и ensemble
  src/training/               точки запуска обучения
  src/inference/              predictor и загрузка артефактов
  service/                    ML FastAPI
  notebooks/                  EDA и эксперименты
  artifacts/                  веса, метрики и manifest моделей
scripts/                      вспомогательные команды
```

## Технологический стек

| Слой | Технологии |
| --- | --- |
| язык | Python 3.10+ |
| API | FastAPI, Uvicorn, Pydantic |
| ML | CatBoost, PyTorch, scikit-learn, pandas, NumPy |
| данные | CSV, временные ряды, признаки расписания и телеметрии |
| транспортный протокол | NDTP поверх TCP, REST для управления эмулятором |
| контейнеризация | Docker, Docker Compose |
| эксперименты | Jupyter Notebook, Kaggle Notebook, Kaggle Dataset |
| контроль версий | Git |

## Быстрый старт

### Backend и эмулятор

Перед запуском положите `ndtp-telemetry-emulator.tar` в `emulator/`. Архив исключён из Git; подробная инструкция и спецификация пакетов находятся в [`emulator/README.md`](emulator/README.md).

```bash
./scripts/start_emulator.sh
```

Конфигурация эмулятора передаётся через `emulator/config.json`. Backend подключается к TCP-порту NDTP и принимает телеметрию от бортовых устройств.

### ML

```bash
cd ml
python -m venv .venv
source .venv/bin/activate
pip install -e '.[dev,models]'
```

Дальнейшие команды обучения и API описаны в [`ml/README.md`](ml/README.md). Пока ML-файлы являются каркасом, поэтому сначала следует согласовать feature contract и target.

## Kaggle

Основной командный notebook:

[Kaggle Notebook — notebook34755737a1](https://www.kaggle.com/code/nezqt52/notebook34755737a1/edit)

Рекомендуемый процесс:

1. хранить код и ноутбуки в Git;
2. запускать EDA и обучение в Kaggle на GPU;
3. сохранять очищенные данные и веса в общей версии Kaggle Dataset;
4. фиксировать commit, seed, список признаков и метрики в `artifact_manifest.json`;
5. скачивать выбранную версию артефактов в `ml/artifacts` перед запуском ML-сервиса.

Настройка команды из трёх человек и правила доступа описаны в [`ml/kaggle/README.md`](ml/kaggle/README.md). Доступ к Notebook и приватному Dataset выдаётся отдельно.

## ML-метрики

Для регрессии задержки отслеживаем MAE/RMSE в секундах. Для классификации — macro-F1, precision/recall критического класса и confusion matrix. Валидация должна быть временной: случайное перемешивание создаёт утечку будущей информации.

Минимальный состав артефакта:

```text
model_catboost.cbm
model_torch.pt
preprocessor.joblib
metrics.json
artifact_manifest.json
```

## API-контуры

Backend отвечает за приём и выдачу транспортных данных. ML-сервис предоставляет:

```text
GET  /health
POST /predict
```

Формат запроса и ответа будет закреплён в `ml/service/schemas.py`. Версия модели должна возвращаться в ответе, чтобы результат можно было воспроизвести и сравнить.

## Ограничения MVP и следующие шаги

- заполнить реальные фамилии команды;
- реализовать загрузку и объединение train-таблиц;
- зафиксировать feature contract и временной split;
- обучить CatBoost/PyTorch и сравнить с baseline;
- подключить артефакты Kaggle к FastAPI;
- добавить тесты parser, features и API;
- вывести дашборд задержек и прогнозов для демо.

## Структура данных

```text
data/raw/train/traffic.csv
data/raw/train/schedule.csv
data/raw/labels/labels_train.csv
data/raw/test/traffic.csv
data/raw/test/schedule.csv
data/raw/validate/schedule_plan.csv
data/raw/validate/points.csv
data/raw/validate/traffic.csv
```

Не коммитьте секреты, `kaggle.json`, API-токены и тяжёлые бинарные веса. Данные и модели должны иметь версию и понятный источник.
