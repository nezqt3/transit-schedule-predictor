# Inference

Загрузка версионированных артефактов и единая функция предсказания для FastAPI-сервиса.
По умолчанию `MODEL_NAME=lightgbm_plan`: сервис загружает пару моделей и
план остановок из `SCHEDULE_PLAN_PATH`. `MODEL_NAME=catboost` оставляет
предыдущую модель доступной для сравнения.
